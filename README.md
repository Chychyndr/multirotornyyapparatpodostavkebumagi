# Мультироторный аппарат по доставке бумаги (Arduino Uno + MX1508 + LM393)

Проект для школьного робота, который едет по **чёрной линии** с помощью **2 датчиков линии LM393** и управляет **DC-моторами** через драйвер **MX1508**.  
Eсть **симулятор на ПК** (PlatformIO `native`) для тестов без робота.

---

## ✅ Возможности

- Следование по линии (2 датчика LM393, цифровой сигнал)
- Универсальная работа с датчиками: поддерживает режимы **“линия = 1”** и **“линия = 0”**
- Режим поиска линии при потере
- Антидребезг (majority filter, окно 1/3/5)
- Поддержка:
  - **Arduino Uno** (`env:uno`)
  - **Симулятор на ПК** (`env:native`)

---

## 🧩 Комплектующие

- Arduino Uno
- Драйвер моторов **MX1508**
- 2× датчик линии **LM393** (цифровой выход DO)
- 2× DC мотор (или 2 стороны шасси)

---

## 🔌 Подключение (пины по умолчанию)

### PWM-пины на Arduino Uno
Важно: на Arduino Uno PWM-пины: **3, 5, 6, 9, 10, 11**.  
Для MX1508 нужно **4 PWM пина** (по 2 на каждый мотор).

### MX1508 → Arduino Uno
**Левый мотор**
- `ML_IN1` → **D5 (PWM)**
- `ML_IN2` → **D6 (PWM)**

**Правый мотор**
- `MR_IN1` → **D9 (PWM)**
- `MR_IN2` → **D10 (PWM)**

### LM393 (DO) → Arduino Uno
- `Left DO` → **A0** (используется как цифровой вход)
- `Right DO` → **A1** (используется как цифровой вход)

> A0/A1 на Uno можно использовать как цифровые входы.

### Питание (важно!)
- Моторы питаются **от отдельного источника** (например 2S/6–9V).
- **Общий GND обязателен**: заземление Arduino должна быть соединена с заземлением питания моторов и драйвера.

---

## 🧠 Архитектура проекта

Проект разделён на части:

- `lib/control` — логика движения по линии (не зависит от железа)
- `lib/motor_mx1508` — управление мотором через MX1508
- `src/main.cpp` — склейка: чтение датчиков + вызов логики + установка скорости моторов
- `sim/sim.cpp` — симулятор сценариев (ПК), чтобы тестировать поведение без робота

---

## 🛠️ Сборка и запуск (PlatformIO)

### 1) Сборка прошивки под Arduino Uno
```bash
pio run -e uno
